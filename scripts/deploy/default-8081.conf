http {
    server {
        listen 80;
        server_name pmo.tracehub.git;

        location /realms/ {
            proxy_pass http://localhost:8090;
        }
        location /api/ {
            proxy_pass http://localhost:8081;
        }
    }
}